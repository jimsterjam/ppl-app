{
  "name": "bro-split-ai-relay",
  "version": "1.0.0",
  "description": "Geschützter Relay zwischen server/ (bro-split-app) und der OpenAI-API. Hält den echten OpenAI-API-Key ausschließlich in seinem eigenen Deployment - siehe DEPLOYMENT.md.",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "node --test *.test.js"
  },
  "license": "ISC",
  "dependencies": {
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "express-rate-limit": "^7.0.0",
    "helmet": "^8.3.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
